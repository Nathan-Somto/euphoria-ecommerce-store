import { ResponsiveContainer,PieChart } from "recharts";

export default function ProductChart() {
    return (
        <ResponsiveContainer>
            <PieChart
            data={[]}
            ></PieChart>
        </ResponsiveContainer>
    )
}